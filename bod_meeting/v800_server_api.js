/** * ======================================================================== * BOD REGISTRATION V8.0 — SERVER API CHO WEB DASHBOARD * ======================================================================== * Thêm file này vào Google Apps Script project (cùng với Code_v782) * Thêm Dashboard.html vào project * Deploy as Web App để truy cập dashboard trên mobile * ======================================================================== */// ===== WEB APP ENTRY POINT =====function doGet(e) {  var page = (e && e.parameter && e.parameter.page) ? e.parameter.page : 'dashboard';  if (page === 'admin') {    return HtmlService.createHtmlOutputFromFile("AdminPage")      .setTitle("BOD Admin Page")      .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);  }  return HtmlService.createHtmlOutputFromFile("Dashboard")    .setTitle("BOD Meeting Dashboard")    .setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL);}// ===== DASHBOARD URL — SINGLE SOURCE OF TRUTH =====// Tập trung 1 hàm duy nhất để toàn hệ thống lấy URL Dashboard mới nhất.// Ưu tiên: 1) cfg_dashboardUrl (Admin override) → 2) Deployed /exec URL → 3) Hardcoded deployment//// LƯU Ý: ScriptApp.getService().getUrl() trả về /dev URL khi gọi từ trigger/editor// → Chỉ editor mở được, BTC bấm CTA sẽ lỗi Google Drive!// → Chỉ dùng khi URL chứa "/exec" (deployed version)// Deployment ID chính — cập nhật mỗi khi đổi deployment chínhvar MAIN_DEPLOYMENT_ID = "AKfycbzWeSuP4GjR9rXeQREcdc5yd4N0dnlHWmpDyBJc5ALxGkKSvofu3fQ0tOzGNz9icV9UTA";/** * Lấy URL Dashboard mới nhất — gọi từ mọi nơi cần link CTA. * @returns {string} URL Dashboard */function getDashboardUrl() {  // 1. Kiểm tra Settings sheet — admin override (ưu tiên cao nhất)  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var configSheetName = (typeof CONFIG !== 'undefined' && CONFIG.SHEET_CONFIG) ? CONFIG.SHEET_CONFIG : "Settings";    var settingsSheet = ss.getSheetByName(configSheetName);    if (settingsSheet) {      var sData = settingsSheet.getDataRange().getValues();      for (var i = 0; i < sData.length; i++) {        var key = (sData[i][0] || "").toString().trim().toLowerCase();        var val = (sData[i][1] || "").toString().trim();        if ((key === "cfg_dashboardurl" || key === "dashboard url" || key === "cfg_dashboard_url") && val) {          return val;        }      }    }  } catch (e) {    Logger.log("getDashboardUrl: Settings read error: " + e.message);  }  // 2. ScriptApp — CHỈ dùng khi URL chứa "/exec" (deployed version, ai cũng mở được)  //    Bỏ qua /dev URL vì chỉ editor mới truy cập được  try {    var url = ScriptApp.getService().getUrl();    if (url && url.indexOf("/exec") > -1) return url;    // Nếu là /dev URL → log cảnh báo, KHÔNG dùng    if (url) Logger.log("getDashboardUrl: Skipped /dev URL (trigger context): " + url);  } catch (e) {    Logger.log("getDashboardUrl: ScriptApp error: " + e.message);  }  // 3. Fallback — dùng deployment ID chính (luôn đúng, ai cũng mở được)  return "https://script.google.com/macros/s/" + MAIN_DEPLOYMENT_ID + "/exec";}/** * Frontend gọi được — Dashboard.html, AdminPage.html, Js_Core.html. * @returns {string} URL Web App */function getScriptAppUrl() {  return getDashboardUrl();}// ===== MỞ DASHBOARD TRONG POPUP =====function showDashboardDialog() {  var html = HtmlService.createHtmlOutputFromFile("Dashboard")    .setWidth(1600)    .setHeight(1000);  SpreadsheetApp.getUi().showModalDialog(html, "BOD Meeting Dashboard");}// ===== API: LỊCH SỬ ĐĂNG KÝ 4 TUẦN CỦA 1 BỘ PHẬN (cho preview) =====function getDeptRegistrationHistory(deptName) {  try {    return _getDeptHistory_(deptName);  } catch(e) {    return { weeks: [], missedWeeks: 0, registered: 0, total: 4, month: '' };  }}// ===== API: LẤY DANH SÁCH NGÀY HỌP =====function getAvailableDates() {  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);  if (!sheet) return [];  var cols = CONFIG.COLUMN_MAP;  var data = sheet.getDataRange().getValues();  var dateMap = {};  for (var i = 1; i < data.length; i++) {    var ngayHop = data[i][cols.ngayHop];    if (ngayHop) {      var search = extractSearchDate(ngayHop);      if (search && !dateMap[search]) {        dateMap[search] = formatNgayHop(ngayHop);      }    }  }  return Object.keys(dateMap)    .sort()    .reverse()    .map(function (s) {      return { display: dateMap[s], search: s };    });}// ===== API: LẤY DỮ LIỆU DASHBOARD =====function getDashboardData(searchDate) {  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);  var result = {    total: 0,    approved: 0,    rejected: 0,    pending: 0,    postponed: 0,    items: [],  };  if (!sheet) return result;  var cols = CONFIG.COLUMN_MAP;  var data = sheet.getDataRange().getValues();  for (var i = 1; i < data.length; i++) {    var row = data[i];    if (searchDate && !matchNgayHop(row[cols.ngayHop], searchDate)) continue;    result.total++;    var status = (row[cols.status] || "").toString().trim();    switch (status) {      case "Duyệt":        result.approved++;        break;      case "Từ chối":        result.rejected++;        break;      case "Hoãn":        result.postponed++;        break;      default:        result.pending++;    }    result.items.push({      row: i + 1,      hoTen: row[cols.hoTen] || "N/A",      boPhan: row[cols.boPhan] || "",      noiDung: row[cols.noiDung] || "",      ngayHop: formatNgayHop(row[cols.ngayHop]),      thoiLuong: formatThoiLuong(row[cols.thoiLuong]),      status: status || "Chờ duyệt",      thamGia: row[cols.thamGia] || "",      daGuiEmail: row[cols.daGuiEmail] || "",      thuTu: parseInt(row[cols.thuTu]) || 0,      ghiChu: row[cols.ghiChu] || "",      tlCD: parseInt(row[cols.thoiLuongChiDao]) || 10,      email: row[cols.email] || "",      emailLienQuan: row[cols.emailLienQuan] || "",      emailSent: !!(row[cols.daGuiEmail]),    });  }  return result;}// ===== API: TRẠNG THÁI QUY TRÌNH 4 BƯỚC =====function getProcessStatus() {  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);  var scheduleSheet = ss.getSheetByName(CONFIG.SHEET_SCHEDULE);  // Tìm thứ 2 sắp tới gần nhất  var now = new Date();  var day = now.getDay(); // 0=CN, 1=T2, ...  var hour = now.getHours();  var diff;  if (day === 1 && hour < 12)    diff = 0; // Thứ 2 sáng = ngày họp  else if (day === 0) diff = 1;  else if (day === 1)    diff = 7; // Thứ 2 chiều → tuần sau  else diff = 8 - day;  var nextMonday = new Date(now);  nextMonday.setDate(now.getDate() + diff);  var d = nextMonday.getDate().toString().padStart(2, "0");  var m = (nextMonday.getMonth() + 1).toString().padStart(2, "0");  var searchDate = d + "/" + m;  var displayDate = "Thứ 2, " + d + "/" + m + "/" + nextMonday.getFullYear();  var result = {    targetDate: displayDate,    searchDate: searchDate,    currentStep: 1,    steps: [      {        id: 1,        name: "Đăng ký",        desc: "Nhân viên submit form (T3→T6)",        status: "pending",        count: 0,      },      {        id: 2,        name: "Duyệt",        desc: "BOD duyệt nội dung (T6→T7)",        status: "pending",        count: 0,      },      {        id: 3,        name: "Lịch trình",        desc: "Tạo & gửi lịch (CN 20:00)",        status: "pending",        count: 0,      },      {        id: 4,        name: "Họp BOD",        desc: "Cuộc họp diễn ra (T2)",        status: "pending",        count: 0,      },    ],  };  if (!sheet) return result;  var cols = CONFIG.COLUMN_MAP;  var data = sheet.getDataRange().getValues();  var total = 0,    approved = 0,    pending = 0;  for (var i = 1; i < data.length; i++) {    if (matchNgayHop(data[i][cols.ngayHop], searchDate)) {      total++;      var st = (data[i][cols.status] || "").toString().trim();      if (st === "Duyệt") approved++;      else if (!st || st === "Chờ duyệt") pending++;    }  }  // Step 1: Đăng ký  result.steps[0].count = total;  if (total > 0) {    result.steps[0].status = "done";    result.currentStep = 2;  } else {    result.steps[0].status = day >= 2 && day <= 5 ? "active" : "pending";    if (day >= 2 && day <= 5) result.currentStep = 1;    return result;  }  // Step 2: Duyệt  result.steps[1].count = approved;  if (pending === 0 && approved > 0) {    result.steps[1].status = "done";    result.currentStep = 3;  } else if (approved > 0 || day >= 5) {    result.steps[1].status = "active";    result.currentStep = 2;  } else {    result.steps[1].status = "pending";  }  // Step 3: Lịch trình  var scheduleCreated = false;  if (scheduleSheet) {    var banner = (scheduleSheet.getRange("A3").getValue() || "").toString();    var schedDate = (scheduleSheet.getRange("A8").getValue() || "").toString();    if (schedDate.indexOf(searchDate) >= 0) {      scheduleCreated = true;      if (banner.indexOf("ĐÃ DUYỆT") >= 0) {        result.steps[2].status = "done";        result.steps[2].count = 1;        result.currentStep = 4;      } else if (banner.indexOf("BẢN NHÁP") >= 0) {        result.steps[2].status = "active";        result.steps[2].count = 1;        result.currentStep = 3;      }    }  }  if (!scheduleCreated && result.steps[1].status === "done") {    result.steps[2].status = day === 0 || day === 6 ? "active" : "pending";    result.currentStep = 3;  }  // Step 4: Họp BOD  if (day === 1 && hour >= 12 && result.steps[2].status === "done") {    result.steps[3].status = "done";  } else if (day === 1 && hour >= 8 && result.steps[2].status === "done") {    result.steps[3].status = "active";    result.currentStep = 4;  }  return result;}// ===== API: TRANG THAI DANG KY THEO BO PHAN =====function getDeptRegistrationStatus(searchDate) {  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);  // ĐỌC TỪ SHEET "Dim bộ phận"  var dimSheet = ss.getSheetByName(CONFIG.SHEET_DIM_DEPT);  var requiredDepts = [];  var deptEmails = {};  var deptContacts = {};  var deptCc = {};  if (dimSheet && dimSheet.getLastRow() > 0) {    var dimData = dimSheet.getDataRange().getValues();    for (var i = 0; i < dimData.length; i++) {      var deptName = (dimData[i][0] || "").toString().trim();      var batBuoc = (dimData[i][1] || "").toString().trim();      if (!deptName || batBuoc !== "Bắt buộc") continue;      var key = deptName.toUpperCase();      requiredDepts.push(deptName);      if (dimData[i].length > 2) deptEmails[key] = (dimData[i][2] || "").toString().trim();      if (dimData[i].length > 3) deptContacts[key] = (dimData[i][3] || "").toString().trim();      if (dimData[i].length > 4) deptCc[key] = (dimData[i][4] || "").toString().trim();    }  }  // BỔ SUNG từ CONFIG.DEPT_CONTACTS  if (CONFIG.DEPT_CONTACTS) {    for (var i = 0; i < requiredDepts.length; i++) {      var key = requiredDepts[i].toUpperCase();      var cfg = CONFIG.DEPT_CONTACTS[key];      if (cfg) {        if (!deptEmails[key]) deptEmails[key] = cfg.email || "";        if (!deptContacts[key]) deptContacts[key] = cfg.contact || "";        if (!deptCc[key] && cfg.cc) deptCc[key] = cfg.cc;      }    }  }  // Count registrations per dept  var deptStats = {};  if (sheet) {    var cols = CONFIG.COLUMN_MAP;    var data = sheet.getDataRange().getValues();    for (var i = 1; i < data.length; i++) {      if (searchDate && !matchNgayHop(data[i][cols.ngayHop], searchDate)) continue;      var bp = (data[i][cols.boPhan] || "").toString().trim().toUpperCase();      if (!deptStats[bp])        deptStats[bp] = { total: 0, approved: 0, pending: 0, rejected: 0 };      deptStats[bp].total++;      var st = (data[i][cols.status] || "").toString().trim();      if (st === "Duyệt") deptStats[bp].approved++;      else if (st === "Từ chối" || st === "Hoãn") deptStats[bp].rejected++;      else deptStats[bp].pending++;    }  }  // ĐẾM SỐ LẦN GỬI NHẮC NHỞ từ Email Log — CHỈ TRONG TUẦN CỦA searchDate  var reminderCounts = {};  var lastSentTimes = {};  try {    // Cắt DD/MM từ đầu vào (vd: '09/03')    var sDate = new Date(); // mặc định dùng today nếu không parse được    if (searchDate) {      var sp = searchDate.trim().split("/");      if (sp.length >= 2) {        var year = sp.length === 3 ? parseInt(sp[2]) : new Date().getFullYear();        sDate = new Date(year, parseInt(sp[1]) - 1, parseInt(sp[0]));      } else {        // Fallback cho format chuẩn        var parsed = new Date(searchDate);        if (!isNaN(parsed.getTime())) sDate = parsed;      }    }        var dayOfWeek = sDate.getDay(); // 0=Sun..6=Sat    var diffToMon = dayOfWeek === 0 ? -6 : 1 - dayOfWeek;    var weekMonday = new Date(sDate);    weekMonday.setDate(sDate.getDate() + diffToMon);    weekMonday.setHours(0, 0, 0, 0);    var weekSunday = new Date(weekMonday);    weekSunday.setDate(weekMonday.getDate() + 6);    weekSunday.setHours(23, 59, 59, 999);    var logSheet = ss.getSheetByName("Email Log");    if (logSheet && logSheet.getLastRow() > 1) {      var logData = logSheet.getDataRange().getValues();      for (var i = 1; i < logData.length; i++) {        var logType = (logData[i][1] || "").toString();        var logDetail = (logData[i][4] || "").toString();        var logTimeRaw = logData[i][0];        if (logType.indexOf("Nhắc") === -1 && logType.indexOf("reminder") === -1) continue;        // Filter theo tuần        var logDate = null;        if (logTimeRaw instanceof Date) {          logDate = logTimeRaw;        } else {          logDate = new Date(logTimeRaw);          if (isNaN(logDate.getTime())) {            var lsp = logTimeRaw.toString().split(/[\s/]+/);            if (lsp.length >= 3) logDate = new Date(lsp[2], parseInt(lsp[1])-1, parseInt(lsp[0]));          }        }        if (!logDate || logDate < weekMonday || logDate > weekSunday) continue;        // Chỉ đếm khi detail BẮT ĐẦU bằng tên bộ phận        for (var j = 0; j < requiredDepts.length; j++) {          var dk = requiredDepts[j].toUpperCase();          var detailUpper = logDetail.toUpperCase();          if (detailUpper.indexOf(dk + " (") === 0 || detailUpper === dk) {            reminderCounts[dk] = (reminderCounts[dk] || 0) + 1;            var ts = "";            if (logTimeRaw instanceof Date) {              var hh = ("0" + logTimeRaw.getHours()).slice(-2);              var mm = ("0" + logTimeRaw.getMinutes()).slice(-2);              var dd = ("0" + logTimeRaw.getDate()).slice(-2);              var mo = ("0" + (logTimeRaw.getMonth()+1)).slice(-2);              ts = hh + ":" + mm + " " + dd + "/" + mo;            } else {              var str = logTimeRaw.toString();              var parts = str.split(" ");              if (parts.length >= 2) {                var dp = parts[0].split("/");                var tp = parts[1].split(":");                ts = (tp[0]||"") + ":" + (tp[1]||"") + " " + (dp[0]||"") + "/" + (dp[1]||"");              } else {                ts = str.substring(0, 16);              }            }            lastSentTimes[dk] = ts;          }        }      }    }  } catch(e) { Logger.log("Log read error: " + e.message); }  var result = [];  for (var i = 0; i < requiredDepts.length; i++) {    var d = requiredDepts[i];    var key = d.toUpperCase();    var stats = deptStats[key] || { total: 0, approved: 0, pending: 0, rejected: 0 };    result.push({      name: d,      email: deptEmails[key] || "",      contact: deptContacts[key] || "",      cc: deptCc[key] || "",      total: stats.total,      approved: stats.approved,      pending: stats.pending,      status: stats.total === 0 ? "missing" : stats.pending > 0 ? "partial" : "ok",      reminderCount: reminderCounts[key] || 0,      lastSent: lastSentTimes[key] || ""    });  }  return result;}// Helper phân loại người Nhậtfunction isJapanesePerson(name, email, dept) {  var e = (email || "").toLowerCase();  var n = (name || "").toLowerCase();  if (e.indexOf(".jp@esuhai.com") !== -1) return true;  var jpNames = ["mishima","oishi","onda","kuriyama","honda","nakamura","sato",                "suzuki","takahashi","ito","watanabe","yamamoto","saito",                "kobayashi","tsurumi","matsumoto","kondo","yamaguchi","kato",                "shimizu","satomura","masuda","utsumi"];  for (var i = 0; i < jpNames.length; i++) {    if (n.indexOf(jpNames[i]) !== -1) return true;  }  return false;}// ===== API: GỬI NHẮC NHỞ 1 BỘ PHẬN (có leo thang) =====function sendDeptReminderWeb(deptName, deptEmail, displayDate, contactName, deptCc, reminderCount) {  if (!deptEmail || !isValidEmail(deptEmail))    return { success: false, msg: "Email không hợp lệ: " + deptEmail };  try {    var count = parseInt(reminderCount) || 1;        // Subject leo thang    var subject;    if (count <= 1) subject = "[BOD MEETING] Nhắc nhở đăng ký báo cáo — " + displayDate;    else if (count === 2) subject = "[BOD MEETING] ⚠️ Nhắc nhở lần 2 — " + deptName + " chưa đăng ký — " + displayDate;    else subject = "[BOD MEETING] 🔴 Nhắc nhở KHẨN lần " + count + " — " + deptName + " — " + displayDate;    var plainBody = "Kinh gui " + (contactName || deptName) + ",\n\n"      + "Bo phan " + deptName + " chua dang ky bao cao cho cuoc hop BOD ngay " + displayDate + ".\n"      + "Day la lan nhac nho thu " + count + ".\n"      + "Han dang ky: Thu Nam, 17:00\n\nTran trong,\nBTC Meeting BOD";    // Build HTML email — LUÔN song ngữ    var htmlBody = "";    try {      htmlBody = buildReminderEmail(deptName, contactName || "", displayDate, "", true, count);    } catch(te) { Logger.log("Template error: " + te.message); }    // CC: luôn CC BTC, lần 3+ thêm thêm nếu cần    var ccList = CONFIG.BTC_FIXED ? CONFIG.BTC_FIXED.join(",") : "";    if (deptCc) {      var extras = deptCc.split(/[,;]+/);      for (var x = 0; x < extras.length; x++) {        var e = extras[x].trim();        if (e && ccList.indexOf(e) === -1) ccList += "," + e;      }    }    if (count >= 3 && CONFIG.BTC_FIXED) {      // Đã có BTC trong ccList rồi, không cần thêm      // Nhưng đảm bảo BOD hosting cũng được CC      var hosting = CONFIG.BOD_HOSTING_DEFAULT || "";      if (hosting && ccList.indexOf(hosting) === -1) ccList += "," + hosting;    }    // Route through N8N    if (CONFIG.EMAIL_METHOD === "n8n") {      var ok = sendViaWebhook({        to: deptEmail,        cc: ccList,        subject: subject,        body: plainBody,        htmlBody: htmlBody,        senderName: CONFIG.EMAIL_SENDER_NAME || "BTC Meeting BOD",        senderEmail: CONFIG.EMAIL_SENDER_ADDRESS || "",        type: "dept_reminder",        deptName: deptName,        displayDate: displayDate,        reminderCount: count      });      if (ok) {        logEmailSend('reminder', 1, deptName + ' (' + deptEmail + ') [Lần ' + count + ']');        return { success: true, msg: "Đã gửi nhắc nhở lần " + count + " đến " + deptName + " (" + deptEmail + ") [N8N]" };      }      Logger.log("N8N failed, falling back to MailApp");    }    // MailApp fallback    var emailOpts = { name: CONFIG.EMAIL_SENDER_NAME || "BTC Meeting BOD", cc: ccList };    if (htmlBody) emailOpts.htmlBody = htmlBody;    MailApp.sendEmail(deptEmail, subject, plainBody, emailOpts);    logEmailSend('reminder', 1, deptName + ' (' + deptEmail + ') [Lần ' + count + ']');    return { success: true, msg: "Đã gửi nhắc nhở lần " + count + " đến " + deptName + " (" + deptEmail + ")" };  } catch (e) {    Logger.log("sendDeptReminderWeb error: " + e.message);    return { success: false, msg: "Lỗi gửi email: " + e.message };  }}// ===== API: GUI NHAC NHO TAT CA BO PHAN CHUA DANG KY =====function sendBulkReminderWeb(searchDate, displayDate) {  var depts = getDeptRegistrationStatus(searchDate);  var sent = 0, failed = 0, msgs = [];  for (var i = 0; i < depts.length; i++) {    if (depts[i].status === "missing" && depts[i].email) {      var r = sendDeptReminderWeb(depts[i].name, depts[i].email, displayDate, depts[i].contact, depts[i].cc);      if (r.success) {        sent++;        msgs.push("✔ " + depts[i].name);      } else {        failed++;        msgs.push("✗ " + depts[i].name);      }      if (i < depts.length - 1) Utilities.sleep(500);    }  }  logEmailSend('bulk_reminder', sent, msgs.join(', '));  return { sent: sent, failed: failed, details: msgs.join("\n") };}// ===== API: GỬI KẾT QUẢ PHÊ DUYỆT TỪ DASHBOARD =====// Same logic as sendApprovalResults() nhưng không dùng ui.prompt// Chống gửi trùng bằng cột daGuiEmailfunction sendApprovalResultEmails() {  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);    if (!sheet) return { success: false, msg: "Không tìm thấy sheet: " + CONFIG.SHEET_RESPONSES };        var cols = CONFIG.COLUMN_MAP;    var data = sheet.getDataRange().getValues();    Logger.log("sendApprovalResultEmails: total rows=" + data.length + ", cols.ngayHop=" + cols.ngayHop);    var sent = 0, skipped = 0, alreadySent = 0, failed = 0;    var btcEmails = getBTCEmails();    var now = new Date();    var timestamp = Utilities.formatDate(now, "Asia/Ho_Chi_Minh", "dd/MM HH:mm");    var details = [];    // Tìm ngày họp gần nhất có đăng ký    var targetDate = null;    for (var i = data.length - 1; i >= 1; i--) {      var d = data[i][cols.ngayHop];      if (d) { targetDate = d; break; }    }    if (!targetDate) return { success: false, msg: "Không tìm thấy đăng ký nào trong sheet " + CONFIG.SHEET_RESPONSES };    // Tạo search key từ targetDate    var searchKey = '';    if (targetDate instanceof Date) {      searchKey = (targetDate.getDate() < 10 ? '0' : '') + targetDate.getDate() + '/' +                   ((targetDate.getMonth()+1) < 10 ? '0' : '') + (targetDate.getMonth()+1);    } else {      searchKey = targetDate.toString().trim();    }    Logger.log("sendApprovalResultEmails: targetDate=" + targetDate + ", searchKey=" + searchKey);    for (var i = 1; i < data.length; i++) {      var row = data[i];      if (!matchNgayHop(row[cols.ngayHop], searchKey)) continue;      var email = (row[cols.email] || "").toString().trim();      var status = (row[cols.status] || "").toString().trim();      var hoTen = toTitleCase(row[cols.hoTen]) || "N/A";      var daGui = (row[cols.daGuiEmail] || "").toString().trim();      // CHỐNG GỬI TRÙNG: skip nếu đã gửi      if (daGui) {        alreadySent++;        details.push("⏭️ " + hoTen + " — đã gửi trước đó");        continue;      }      if (!email || !isValidEmail(email)) { skipped++; continue; }      if (!status || status === "Chờ duyệt") { skipped++; continue; }      var emailInput = {        hoTen: hoTen,        noiDung: row[cols.noiDung] || "",        ngayHop: formatNgayHop(row[cols.ngayHop]),        boPhan: row[cols.boPhan] || "",        ghiChu: row[cols.ghiChu] || ""      };      // Build plain text email (fallback)      var emailData;      if (status === "Duyệt") emailData = buildEmailApproved(emailInput);      else if (status === "Từ chối") emailData = buildEmailRejected(emailInput);      else if (status === "Hoãn") emailData = buildEmailPostponed(emailInput);      else { skipped++; continue; }      // Build HTML email using v820 template      var htmlBody = "";      try {        htmlBody = buildApprovalResultEmail(          hoTen,          emailInput.ngayHop,          status,          emailInput.noiDung,          emailInput.ghiChu        );      } catch(te) { Logger.log("HTML template error: " + te.message); }      // CC: luôn CC BTC      var ccList = btcEmails.all.slice();      var emailLQ = row[cols.emailLienQuan];      if (emailLQ) {        emailLQ.toString().split(/[,;\n]+/).forEach(function(e) {          e = e.trim().toLowerCase();          if (isValidEmail(e)) ccList.push(e);        });      }      if (sent > 0) Utilities.sleep(CONFIG.EMAIL_DELAY_MS);      if (sendEmail({        to: email,        cc: ccList.filter(function(v, idx, arr) { return arr.indexOf(v) === idx; }).join(","),        subject: emailData.subject,        body: emailData.body,        htmlBody: htmlBody      })) {        sent++;        details.push("✔︎ " + hoTen + " (" + status + ") → " + email);        sheet.getRange(i + 1, cols.daGuiEmail + 1).setValue("✔︎ " + timestamp);      } else {        failed++;        details.push("❌ " + hoTen + " → gửi thất bại");      }    }    if (sent === 0 && skipped === 0 && failed === 0 && alreadySent === 0) {      return { success: false, msg: "Không tìm thấy đăng ký nào cho ngày " + searchKey };    }    var msg = "✔︎ Gửi mới: " + sent;    if (alreadySent > 0) msg += " | ⏭️ Đã gửi trước: " + alreadySent;    if (failed > 0) msg += " | ❌ Lỗi: " + failed;    if (skipped > 0) msg += " | Bỏ qua: " + skipped;        logEmailSend('approval_result', sent, msg);    return { success: sent > 0 || alreadySent > 0, msg: msg, details: details };  } catch (e) {    return { success: false, msg: "Lỗi: " + e.message };  }}// ===== API: GUI NHAC PHE DUYET =====function sendApprovalReminderWeb(searchDate, displayDate) {  var btcEmails = getBTCEmails();  var stats = getDetailedStats(searchDate);  if (stats.pending === 0)    return { success: false, msg: "Khong co dang ky nao cho duyet!" };  try {    var subject = "[BOD MEETING] Nhac phe duyet noi dung - " + displayDate;    var plainBody = "Kinh gui Ban To Chuc,\n\nHien con " + stats.pending + " dang ky cho phe duyet cho cuoc hop BOD ngay " + displayDate + ".\nTong: " + stats.total + " | Duyet: " + stats.approved + " | Cho: " + stats.pending + "\n\nTran trong,\nBTC Meeting BOD";    // Build HTML email from templates    var htmlBody = "";    try { htmlBody = buildApprovalReminderEmail(displayDate, stats.pending); } catch(te) { Logger.log("Template error: " + te.message); }    // Route through N8N if configured    if (CONFIG.EMAIL_METHOD === "n8n") {      var ok = sendViaWebhook({        to: btcEmails.all.join(","),        subject: subject,        body: plainBody,        htmlBody: htmlBody,        senderName: "BTC Meeting BOD",        senderEmail: CONFIG.EMAIL_SENDER_ADDRESS || "",        type: "approval_reminder",        stats: { total: stats.total, approved: stats.approved, pending: stats.pending },        displayDate: displayDate,      });      if (ok) {        logEmailSend('approval_reminder', 1, stats.pending + ' cho duyet');        return { success: true, msg: "Da gui nhac phe duyet den BTC (" + stats.pending + " cho duyet) [N8N]" };      }      Logger.log("N8N failed, falling back to MailApp");    }    // MailApp fallback    var emailOpts = { name: "BTC Meeting BOD" };    if (htmlBody) emailOpts.htmlBody = htmlBody;    MailApp.sendEmail(btcEmails.all.join(","), subject, plainBody, emailOpts);    logEmailSend('approval_reminder', 1, stats.pending + ' cho duyet');    return { success: true, msg: "Da gui nhac phe duyet den BTC (" + stats.pending + " cho duyet)" };  } catch (e) {    Logger.log("sendApprovalReminderWeb error: " + e.message);    return { success: false, msg: "Loi: " + e.message };  }}// ===== API: XEM LICH TRINH PREVIEW =====function getSchedulePreview(searchDate) {  var ss = SpreadsheetApp.getActiveSpreadsheet();  var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);  var scheduleSheet = ss.getSheetByName(CONFIG.SHEET_SCHEDULE);  var result = {    created: false,    confirmed: false,    items: [],    totalMinutes: 0,    ngayHop: "",  };  // Check if schedule already exists  if (scheduleSheet) {    var banner = (scheduleSheet.getRange("A3").getValue() || "").toString();    var schedDate = (scheduleSheet.getRange("A8").getValue() || "").toString();    if (schedDate.indexOf(searchDate) >= 0) {      result.created = true;      result.confirmed = banner.indexOf("ĐÃ DUYỆT") >= 0;      result.ngayHop = schedDate;      // Read existing schedule      for (var i = CONFIG.AGENDA_START_ROW; i <= CONFIG.AGENDA_END_ROW; i++) {        var content = scheduleSheet.getRange(i, 3).getValue();        if (content && content.toString().trim()) {          result.items.push({            stt: scheduleSheet.getRange(i, 1).getValue(),            time: formatTime(scheduleSheet.getRange(i, 2).getValue()),            content: content.toString(),            presenter: (              scheduleSheet.getRange(i, 4).getValue() || ""            ).toString(),            tlTB: (scheduleSheet.getRange(i, 5).getValue() || "").toString(),            tlCD: (scheduleSheet.getRange(i, 6).getValue() || "").toString(),            related: (scheduleSheet.getRange(i, 7).getValue() || "").toString(),          });        }      }      return result;    }  }  // Build preview from approved registrations  if (!sheet) return result;  var cols = CONFIG.COLUMN_MAP;  var data = sheet.getDataRange().getValues();  var emailToName = loadEmailToNameMap();  var items = [];  for (var i = 1; i < data.length; i++) {    var row = data[i];    if (!matchNgayHop(row[cols.ngayHop], searchDate)) continue;    if ((row[cols.status] || "").toString().trim() !== "Duyệt") continue;    var tlTB = parseThoiLuong(row[cols.thoiLuong]);    if (tlTB === 0) tlTB = 10;    var tlCD = parseThoiLuong(row[cols.thoiLuongChiDao]);    if (tlCD === 0) tlCD = getDefaultChiDaoTime(row[cols.boPhan]);    var tenLQ = row[cols.tenLienQuan] || "";    if (!tenLQ && row[cols.emailLienQuan])      tenLQ = lookupNamesFromEmailList(row[cols.emailLienQuan], emailToName);    items.push({      thuTu: parseInt(row[cols.thuTu]) || 999,      content:        "[" + (row[cols.boPhan] || "") + "] " + (row[cols.noiDung] || ""),      presenter: toTitleCase(row[cols.hoTen] || ""),      tlTB: tlTB,      tlCD: tlCD,      related: tenLQ || "",    });  }  items.sort(function (a, b) {    return a.thuTu - b.thuTu;  });  var t = 8 * 60 + 30;  for (var i = 0; i < items.length; i++) {    items[i].stt = i + 1;    items[i].time =      String(Math.floor(t / 60)).padStart(2, "0") +      ":" +      String(t % 60).padStart(2, "0");    // Tính toán trước khi convert sang string (fix Bug 3)    var numTB = items[i].tlTB;    var numCD = items[i].tlCD;    result.totalMinutes += numTB + numCD;    t += numTB + numCD;    items[i].tlTB = numTB + "'";    items[i].tlCD = numCD + "'";  }  result.items = items;  return result;}// ===== API: TAO LICH TRINH TU DASHBOARD =====function createScheduleFromDashboard(searchDate) {  try {    generateSchedule();    return { success: true, msg: "Đã tạo lịch trình!" };  } catch (e) {    return { success: false, msg: "Lỗi: " + e.message };  }}// ===== API: GUI LICH TRINH TU DASHBOARD =====function sendScheduleFromDashboard() {  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var sheet = ss.getSheetByName(CONFIG.SHEET_RESPONSES);    if (!sheet) return { success: false, msg: "Không tìm thấy sheet đăng ký" };    var cols = CONFIG.COLUMN_MAP;    var data = sheet.getDataRange().getValues();    // Tìm ngày họp gần nhất    var targetDate = null;    for (var i = data.length - 1; i >= 1; i--) {      var d = data[i][cols.ngayHop];      if (d) { targetDate = d; break; }    }    if (!targetDate) return { success: false, msg: "Không tìm thấy đăng ký nào" };    var searchKey = '';    if (targetDate instanceof Date) {      searchKey = (targetDate.getDate() < 10 ? '0' : '') + targetDate.getDate() + '/' +                   ((targetDate.getMonth()+1) < 10 ? '0' : '') + (targetDate.getMonth()+1);    } else {      searchKey = targetDate.toString().trim();    }    var displayDate = formatNgayHop(targetDate);    // Build schedule items từ approved registrations    var items = [];    var emails = new Set();    var btcEmails = getBTCEmails();    btcEmails.all.forEach(function(e) { emails.add(e.toLowerCase()); });    emails.add("leson@esuhai.com");    try {      var hosting = getBodHosting();      if (hosting && hosting.email) emails.add(hosting.email.toLowerCase());    } catch(he) {}    for (var i = 1; i < data.length; i++) {      var row = data[i];      if (!matchNgayHop(row[cols.ngayHop], searchKey)) continue;      if (row[cols.status] !== "Duyệt") continue;      var thuTu = parseInt(row[cols.thuTu]) || 999;      var tlTB = parseThoiLuong(row[cols.thoiLuong]) || 10;      var tlCD = parseThoiLuong(row[cols.thoiLuongChiDao]) || 10;      items.push({        stt: thuTu,        content: (row[cols.noiDung] || "").toString(),        presenter: (row[cols.hoTen] || "").toString(),        dept: (row[cols.boPhan] || "").toString(),        tlTB: tlTB,        tlCD: tlCD,        time: "" // will be calculated      });      var e = (row[cols.email] || "").toString().trim().toLowerCase();      if (isValidEmail(e)) emails.add(e);      if (row[cols.emailLienQuan]) {        row[cols.emailLienQuan].toString().split(/[,;\n]+/)          .map(function(x) { return x.trim().toLowerCase(); })          .filter(function(x) { return isValidEmail(x); })          .forEach(function(x) { emails.add(x); });      }    }    if (items.length === 0)      return { success: false, msg: "Không có nội dung nào được duyệt" };    // Sort theo thuTu    items.sort(function(a, b) { return a.stt - b.stt; });    // Calculate times (08:30 start)    var t = 8 * 60 + 30;    for (var i = 0; i < items.length; i++) {      items[i].stt = i + 1;      items[i].time = (Math.floor(t / 60) < 10 ? '0' : '') + Math.floor(t / 60) + ':' +                       (t % 60 < 10 ? '0' : '') + (t % 60);      t += items[i].tlTB + items[i].tlCD;    }    // Build HTML using v820 template    var htmlBody = buildScheduleEmail(displayDate, items);    // Plain text fallback    var plainBody = "LỊCH TRÌNH CUỘC HỌP BOD — " + displayDate + "\n\n";    for (var i = 0; i < items.length; i++) {      var it = items[i];      plainBody += it.stt + ". " + it.time + " — " + it.content + " (" + it.presenter + ")\n";    }    plainBody += "\n---\nBTC Meeting BOD — ESUHAI GROUP";    // Send    var toList = Array.from(emails).join(",");    var subject = "[BOD Meeting] Lịch trình cuộc họp — " + displayDate + " / 議事スケジュール";    var sent = sendEmail({      to: toList,      subject: subject,      body: plainBody,      htmlBody: htmlBody    });    if (sent) {      logEmailSend('schedule', emails.size, items.length + ' nội dung');      return { success: true, msg: "Đã gửi lịch trình " + items.length + " mục đến " + emails.size + " người!" };    } else {      return { success: false, msg: "Gửi email thất bại" };    }  } catch (e) {    Logger.log("sendScheduleFromDashboard error: " + e.message);    return { success: false, msg: "Lỗi: " + e.message };  }}// ===== API: CẬP NHẬT EMAIL ĐẠI DIỆN BỘ PHẬN =====function updateDeptEmail(deptName, newEmail) {  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var updated = false;        // Cập nhật Dimension bộ phận (nguồn chính cho UI)    var dimSheet = ss.getSheetByName(CONFIG.SHEET_DIM_DEPT);    if (dimSheet) {      var dimData = dimSheet.getDataRange().getValues();      for (var d = 1; d < dimData.length; d++) {        var dp = (dimData[d][0] || "").toString().trim().toUpperCase();        if (dp === deptName.toUpperCase()) {          dimSheet.getRange(d + 1, 3).setValue(newEmail); // Cột C          updated = true;          break;        }      }    }    // Cập nhật Danh sách nhân sự (nguồn fallback)    var hrSheetName = (typeof CONFIG !== 'undefined' && CONFIG.SHEET_HR) ? CONFIG.SHEET_HR : "Nhân sự bắt buộc";    var hrSheet = ss.getSheetByName(hrSheetName) || ss.getSheetByName("DANH SÁCH NHÂN VIÊN");        if (hrSheet) {      var colDept  = (CONFIG.HR_COL_DEPT || 4) - 1;      var colEmail = (CONFIG.HR_COL_EMAIL || 6);       var data = hrSheet.getDataRange().getValues();      for (var i = 1; i < data.length; i++) {        var bp = (data[i][colDept] || "").toString().trim().toUpperCase();        if (bp === deptName.toUpperCase()) {          hrSheet.getRange(i + 1, colEmail).setValue(newEmail);          updated = true;          break;        }      }    }    if (!updated) {      return { success: false, msg: "Không tìm thấy bộ phận (Dim bộ phận & HR): " + deptName };    }    return { success: true, msg: "Đã cập nhật email " + deptName + " → " + newEmail };  } catch (e) {    Logger.log("updateDeptEmail error: " + e.message);    return { success: false, msg: "Lỗi: " + e.message };  }}// ===== API: LẤY URL FORM ĐĂNG KÝ TỪ CẤU HÌNH =====function getFormUrl() {  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var configSheet = ss.getSheetByName(CONFIG.SHEET_CONFIG);    if (!configSheet) return "";    var data = configSheet.getDataRange().getValues();    for (var i = 0; i < data.length; i++) {      var key = (data[i][0] || "").toString().trim().toLowerCase();      var val = (data[i][1] || "").toString().trim();      if (key.indexOf("form") >= 0 && key.indexOf("url") >= 0 && val) {        return val;      }      if (key.indexOf("form đăng ký") >= 0 && val) {        return val;      }    }    return "";  } catch (e) {    Logger.log("getFormUrl error: " + e.message);    return "";  }}// ===== API: RESET SỐ LẦN NHẮC NHỞ (XÓA LOG TRONG TUẦN) =====function resetReminderCountsWeb() {  try {    var ss = SpreadsheetApp.getActiveSpreadsheet();    var logSheet = ss.getSheetByName("Email Log");    if (!logSheet || logSheet.getLastRow() <= 1)      return { success: true, msg: "Không có log nhắc nhở để xóa" };    // Tính Monday-Sunday cho tuần hiện tại    var now = new Date();    var dayOfWeek = now.getDay();    var diffToMon = dayOfWeek === 0 ? -6 : 1 - dayOfWeek;    var weekMonday = new Date(now);    weekMonday.setDate(now.getDate() + diffToMon);    weekMonday.setHours(0, 0, 0, 0);    var weekSunday = new Date(weekMonday);    weekSunday.setDate(weekMonday.getDate() + 6);    weekSunday.setHours(23, 59, 59, 999);    var logData = logSheet.getDataRange().getValues();    var rowsToDelete = [];    for (var i = 1; i < logData.length; i++) {      var logType = (logData[i][1] || "").toString();      if (logType.indexOf("Nhắc") === -1 && logType.indexOf("reminder") === -1) continue;      var logTime = logData[i][0];      var logDate = (logTime instanceof Date) ? logTime : new Date(logTime);      if (logDate >= weekMonday && logDate <= weekSunday) {        rowsToDelete.push(i + 1); // Sheet row (1-indexed, +1 for header)      }    }    // Xóa từ dưới lên để không bị lệch index    for (var r = rowsToDelete.length - 1; r >= 0; r--) {      logSheet.deleteRow(rowsToDelete[r]);    }    return { success: true, msg: "Đã reset " + rowsToDelete.length + " lần nhắc nhở trong tuần" };  } catch (e) {    Logger.log("resetReminderCounts error: " + e.message);    return { success: false, msg: "Lỗi: " + e.message };  }}